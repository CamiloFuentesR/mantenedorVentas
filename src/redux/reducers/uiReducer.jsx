
const initialState = {
    loading: null
}

export const uiReducer = (state = initialState,action) => {
    switch (action.type) {
            
    
        default:
            return state;
    }

}